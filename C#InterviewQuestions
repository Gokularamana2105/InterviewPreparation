Asp.net core MVC & Web API
-------------------------------
    
1.What is Dependency Injection ?
  DI is a technique where object gets it dependencies from outside that it' means (injected) instead of creating them internally. It promotes loose coupling , easier code maintenance.

 Example :

   Without DI
        Created One Class like Email Service into that class Create some methods like SendEmail() with some logic
		and access that method in another class throughout that emailService class so declare the constructor with new EmailService();
		so this is tight coupling and difficult to handle unit test, hard replaces of implementation, changes in one class may affect others.
		
	With DI 
	    Create One Interface like IEmail Service with SenEmail() method declaration only and implement that method in concrete class of EmailService
		and access that method in another class throughtout that Interface IEmailService so declare the constructor with DI
        so this loose coupling and easy to handle unit test , and changes implementation very easy and changes in one place not affect that reference class of that interface.	



2. MVC vs Web API
		ASP.NET MVC returns views (HTML ) for browsers; Web API is built for returning data (JSON/XML) to any client (SPA, mobile, other services). 
		In ASP.NET Core, both are unified under one framework — Controller for views, ControllerBase for APIs.
	
3.What are Singleton, Scoped, and Transient?

   ASP.NET Core DI Lifetimes

		1. Transient

		- A new instance is created every time the service is requested.
		- Used for lightweight and stateless services.
		- Each class receives a different object.

		Example:

		builder.Services.AddTransient<IEmailService, EmailService>();

		Example:
		Controller
			|
			|---- EmailService (Object 1)
			|
			|---- EmailService (Object 2)

		service1 != service2

		Real-time examples:
		- Email sender
		- PDF generator
		- File converter
		- QR code generator

		Interview Answer:
		"Transient creates a new instance every time it is requested."


--------------------------------------------------


		2. Scoped

		- One instance is created per HTTP request.
		- All services inside the same request share the same instance.
		- A new instance is created for the next request.

		Example:

		builder.Services.AddScoped<IOrderService, OrderService>();

		Request 1:

		Controller
			|
		Service
			|
		Repository
			|
		Same DbContext instance


		Request 2:

		New DbContext instance


		Real-time example:

		E-commerce Order Flow:

		Controller
			|
		OrderService
			|
		PaymentService
			|
		InventoryService
			|
		Repository


		All use the same DbContext during one request.

		Why DbContext is Scoped?

		One Request
			  |
		One Transaction
			  |
		One DbContext


		Interview Answer:
		"Scoped creates one instance per HTTP request and is mainly used for DbContext because it maintains consistency within a request."


--------------------------------------------------


		3. Singleton

		- Only one instance is created for the entire application.
		- The same object is shared by all users and requests.
		- Object exists until the application stops.

		Example:

		builder.Services.AddSingleton<ICacheService, CacheService>();

		Application Starts
				|
		Create Object
				|
		All Users Share Same Object
				|
		Application Stops


		Real-time examples:
		- Memory Cache
		- Configuration Service
		- Application Settings


		Interview Answer:
		"Singleton creates a single instance for the entire application lifetime and shares it with all users."


		--------------------------------------------------


		DI Lifetime Comparison

		Transient:
		- New object every time
		- Short lifetime
		- Stateless services


		Scoped:
		- One object per HTTP request
		- Request lifetime
		- DbContext


		Singleton:
		- One object for entire application
		- Application lifetime
		- Cache and configuration


		Easy Way to Remember:

		Transient  -> New every time

		Scoped     -> One per request

		Singleton  -> One for whole application
												
	
	3. What is the difference between Scoped and Singleton?
		Answer :
		Scoped	                                                   Singleton
		One instance per request              One instance for the entire application
		Destroyed after request	              Lives until application stops
		Used for DbContext and 
		business services	                      Used for caching and configuration

		Example:

		In my project, DbContext is Scoped because each request should have its own database transaction. 
		CacheService is Singleton because all users can share the same cached data.
		
	4. What is the difference between Scoped and Transient?

			Answer:

			Scoped creates one instance for the entire HTTP request.

			Transient creates a new instance every time it is injected.

			Example:

			Request
			   |
			Controller
			   |
			Service A
			   |
			Repository


			With Scoped:

			All classes receive the same object instance during the same HTTP request.


			With Transient:

			Each class gets a different object instance every time it is injected.
			
			
	5. Why is DbContext registered as Scoped?

		Answer:

		DbContext tracks entity changes during a request.
		Registering it as Scoped ensures all repositories and services in the same request share the same DbContext and transaction.
		Example:

		Update Product

			  ↓

		Update Stock

			  ↓

		Insert Audit Log

			  ↓

		SaveChanges() in the same time 


		All operations use one DbContext instance.
		
	6.  Why shouldn't DbContext be Singleton?

			Answer:

			DbContext should not be registered as Singleton because it is not thread-safe.

			A Singleton DbContext creates only one instance for the entire application.

			If multiple users use the application at the same time, they will share the same DbContext object, which can cause problems.

			Real-time Example:

			Imagine an online shopping website.

			User A:
			- Opens product page
			- Changes Product Name from "Laptop" to "Gaming Laptop"

			User B:
			- Opens the same product page
			- Changes Product Name from "Laptop" to "Office Laptop"


			If DbContext is Singleton:

			User A and User B use the same DbContext instance.

			Possible problems:

			- User A changes may affect User B changes
			- Entity tracking conflicts can happen
			- Wrong data may be saved
			- Application can throw exceptions
			
			
	7. When would you use a Transient service?
			I use Transient for stateless services such as:

			EmailService
			SmsService
			PdfGenerator
			ExcelExporter
			QRCodeGenerator

			These services don't maintain any data between requests. So it will create new instance everytime the service is requested.
			
	
	
	8. In your project, which services are Singleton, Scoped, and Transient?
		In my ASP.NET Core MVC project:

		Scoped:

		DbContext
		ProductService
		UserService
		OrderRepository

		Transient:

		EmailService
		SmsService
		PdfGenerator
		ExcelExporter

		Singleton:

		CacheService
		ConfigurationService
		JWT Token Configuration
			
			
	9. Why did you choose Scoped for ProductService?


		Because ProductService depends on ProductRepository, which uses DbContext.

		Since DbContext is Scoped, ProductService should also be Scoped to ensure the same database context is used throughout HTTP request.
		
	10. Can multiple users share the same Scoped instance?
	
	   No. Because Each HTTP request gets its own Scoped instance.
	   Example:

			User A

			↓

			DbContext A

			------------------

			User B

			↓

			DbContext B

			The two users never share the same Scoped object.
			
	11. If one HTTP request calls three services, how many DbContext instances are created?

			Only one.

			Example:

			Request

			↓

			OrderService

			↓

			PaymentService

			↓

			InventoryService

			↓

			DbContext

			All services share the same DbContext because it is Scoped.
			
			
	12. What happens if DbContext is registered as Transient?


			Each services gets a new DbContext.

			Example:

			Controller

			↓

			DbContext1

			↓

			Repository

			↓

			DbContext2

			It will cause Problems which are:

			Different entity tracking
			Multiple database connections
			Transaction inconsistency
			SaveChanges() may not include all changes

			Therefore, DbContext should be Scoped.
			
  
What is asp.net core ?
	ASP.NET Core is a cross-platform framework from Microsoft used to build web applications, Web APIs, and backend services.
	It is fast, lightweight, and supports features like Dependency Injection, Middleware, Routing, and Authentication.
	For example, we can use ASP.NET Core to build a Web API for an Angular application.


.net framework vs .net :
  .NET Framework is the older Microsoft platform, mainly used for Windows applications and older ASP.NET applications.
  .NET is the newer, cross-platform platform that runs on Windows, Linux, and macOS.
  .NET is faster, more modern, and supports technologies like ASP.NET Core, Web API, and modern cloud applications.
  So, .NET Framework = older and mainly Windows, .NET = modern and cross-platform.


What is Kestrel :
  Kestrel is the default cross-platform web server used by ASP.NET Core applications. 
  It receives HTTP requests and sends responses back to the client.
  For example, when we run an ASP.NET Core Web API,
  Kestrel listens on a port like https://localhost:5001 and handles the requests.
  
  
1.Program.cs :
  Program.cs is the main entry point of an ASP.NET Core application. It is used to configure services, dependency injection, middleware, routing, and the application pipeline.
For example, we register services using builder.Services and configure middleware using app.Use...() in Program.cs.


2. appsettings.json :
 
  appsettings.json is configuration file used to store application settings in an asp.net core application .
  It can contain connection strings,logging settings, jwt settings and api url's and other configuration values.
  We can access these values using IConfiguration or options pattern.


3. What is dependency injection ?
   

----Sql Inteview Questions--

1.ClusteredIndex & NonClusteredIndex

    *)Index :
	    
		An index basically improve query performance. Normally, if SQL Server doesn't have an index, it reads the entire table to find the required data, which is called a table scan. 
		An index creates a B-Tree structure, so SQL Server can quickly locate the required rows instead of scanning all the records.
		 
	*)ClusteredIndex : 
	
	     A clustered index uses a B-Tree structure with three levels: Root Page, Intermediate Page, and Leaf Page. 
		 The Root Page acts as the entry point and contains pointers to Intermediate Pages.
		 Intermediate Pages further narrow down the search based on key ranges.
		 The Leaf Pages contain the actual table data in sorted order. 
		 When a query searches for a value, SQL Server starts from the Root Page, traverses the Intermediate Pages, and finally reaches the correct Leaf Page,
		 which minimizes the number of page reads and improves performance.
	   
	     
		 Syntax: Create clustered index index_name on table_name(columnname)


2.What is the difference between DELETE, TRUNCATE, and DROP

	 DELETE is used to remove specific records using a WHERE clause, and the table structure remains. 
	 TRUNCATE removes all records quickly without a WHERE clause and resets the identity value, but keeps the table. 
	 DROP removes the entire table, including its data and structure. 
	 In my project, I used DELETE for business data changes, TRUNCATE for clearing test data, and DROP only during development or database changes.


3.What is the difference between WHERE and HAVING clause?

   The main difference is that WHERE  mainly userd for filters individual rows before grouping, 
   while HAVING filters grouped results after GROUP BY. it is mainly used with aggregate functions like COUNT(), SUM(), AVG(), MAX(), and MIN().
   In my project, I used WHERE to filter records such as company, status, or date range, 
   and HAVING in reports to show only groups that met conditions, like providers with total claim amounts above a certain value.
   
   Interview Follow-up Question :
   
   Can we use WHERE and HAVING in the same query?
   
      Yes. Where filters the rows first, then group by creates the groups, and finally HAVING filters those grouped results.
	  
	  
4. What are the different types of joins in SQL Server? Explain each with an example.

   SQL JOIN is used to combine data from multiple related tables based on a common column.
   this mainly six types which are :
   
    INNER JOIN :
	    Returns only the matching records from both tables.
		Project : I mostly used INNER JOIN to display claim details with patient information because I only needed records that existed in both tables.
		
	Left JOIN :
	    Returns all records from the left table and matching records from the right table.
	    If there is no match, NULL is returned.	
	  Usage:
    	I used LEFT JOIN in reports where I wanted to display all claims, even if some related provider or payment information was not available yet.
   
   
   Right JOIN:
        Returns all records from the right table and matching records from the left table.
       Usage:
	      I have rarely used right join because the same result already achieved using left join by changing the table order.
		 
    FULL OUTER JOIN :
	    Returns all records from both tables. If there is no match, NULL is returned.
        I used FULL JOIN occasionally for reconciliation reports where I needed to identify missing records from either side.
		
	Cross JOIN:	  
        Returns every possible combination of rows from both tables.
    Real-Time Use : 
        I have never used CROSS JOIN in my project because it produces a Cartesian product and can generate a very large number of rows.
		
	SELF JOIN	:
	    A table is joined with itself. Usually used for hierarchical data.
	   Usage :
	     I haven't used a SELF JOIN in my healthcare project because we didn't have hierarchical data like employee-manager relationships.


5. What is a Primary Key vs Unique Key? Can a table have multiple unique keys?
   Primary Key : 
		A Primary Key and a Unique Key both ensure uniqueness, but there are some differences.
		A Primary Key uniquely identifies each record in a table and does not allow NULL values. Every table can have only one Primary Key.
		
    Unique Key :
	   A Unique Key also prevents duplicate values, but it allows one NULL value in SQL Server, and a table can have multiple Unique Keys.
	
		In my healthcare claims project, I used the Primary Key for columns like ClaimId to uniquely identify each claim, and 
		Unique Keys for business fields such as ClaimNumber or NPI number  to prevent duplicate business data. 
	

6.What is a Foreign Key and why is it used?

   A Foreign Key is a column that creates a relationship between two tables by referencing the Primary Key of another table.
   Its main purpose is to maintain referential integrity, which means it prevents invalid records from being inserted.
   
    In healthcare claims project, the claims table contain the patientid as a foreign key which  referenced the patientTable. This ensure that a claim could only be created for existing patient ,
	if someone tried to insert new claim with invalid patientid, SQL server would reject it. So it helping maintain data consistency.
	
	
7. What is normalization? Explain 1NF, 2NF, 3NF with examples.

    Normalization is organizing data into multiple related tables to eliminate data redundancy and improve data consistency. 
	It helps avoid duplicate data , update anomalies and improve database maintenance. The most commonly used normal forms are 1NF, 2NF, and 3NF.
	
	update anomalies means Updating the same data in multiple places can cause inconsistencies.
	1NF (First Normal Form) 
	   Rule For :
	   *)Each column should contain only atomic (single) values.
       *)No repeating groups or multiple values in one column.
	
	2 NF (Second Normal Form)
	   Rule For:
	    *)Must satisfy 1NF.
        *) Every non-key column should depend on the entire Primary Key, not just part of it and remove partial dependency.
		
	3. 3NF (Third Normal Form)
		Rule For :
		*)Must satisfy 2NF.
		*)Remove transitive dependency, meaning non-key columns should not depend on another non-key column.
		
	In my healthcare claims project, patient details, provider details, insurance companies, and claim information were stored in separate tables.
	The Claims table stored only IDs like PatientId and ProviderId, and we used JOINs to fetch complete information. 
	This reduced duplicate data and ensured that updating patient or provider details in one place was reflected throughout the application.
	
8. What is denormalization and when would you use it?
      Denormalization is used for adding redundant data to a database to improve query performance by reducing the JOIN's operations. While normalization reduces the redundancy but denormalization 
	  intentionally introduces some duplication for faster data retrieval. It's mainly used in reporting or read-heavy applications where fast retrieval is more important 
	  than eliminating duplicate data.
	  
9. What is the difference between CHAR, VARCHAR, and NVARCHAR?

    CHAR is stores to fixed length data , it always received the specified number of characters even if the value is shorter.  VARCHAR stores variable-length non-unicode data 
	and uses only the required storage , suitable for English text. NVARCHAR stores variable-length and Unicode data which supports multiple languages such as Tamil, Hindi .
	Japanese.

 
 

